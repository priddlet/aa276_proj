# DeepReach BRT (replaces hj_reachability grid solve).
#   pip install -r requirements.txt
#   pip install -r requirements-deepreach.txt
#   pip install torch  # platform-specific wheel from pytorch.org

torch>=2.2
tqdm>=4.66
ConfigArgParse>=1.7
tensorboard>=2.16
scikit-learn>=1.4
