# Legacy HJ reachability (replaced by DeepReach)

The grid-based `hj_reachability` pipeline (`hj_koz_brt.py`, etc.) is kept for reference only.
The active BRT path is `simulation.brt` (DeepReach neural value function).
